## 運用ルール
- 上限：150行を超えたら圧縮
- 圧縮：長文は「結論1行＋理由1行」にする（削除ではない）
- 詳細が必要なら：./.codex/spec/ に逃がし、ここにリンクを残す

## 仕様（目次）
- 目的：Unity Netcodeを使った2Dマルチプレイヤー基盤を整備する
- ゴール（成功条件）：名前入力→キャラ選択→Host/Client選択→移動開始まで一連で動作する
- スコープ（やる/やらない）：
  - やる：UI3ステップ遷移、ネットワーク開始、プレイヤー名/キャラ同期、移動
  - やらない：認証、データ永続化、複雑なゲームルール、本番運用設計
- 重要ルール（前提）：
  - ステート駆動で画面遷移する（InputName/SelectCharacter/SelectNetwork/Play）
  - 依存方向は外→内を維持する（UI・Netcode側から状態制御へ）
  - 変更は小さく行い、毎回確認する
- 未確定TODO：
  - 対応プラットフォームの優先順位
  - 最大同時接続数
  - 受け入れ性能目標（FPSや遅延目安）
- 詳細リンク：
  - ./.codex/spec/（必要になったら作る）
